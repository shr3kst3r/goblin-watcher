Research task — investigate this and report back. Do not implement it.

{ticket_id}: {title}

{repos_block}

{description}

{addition_block}This session is read-only with respect to everything outside this machine.

You may: read any file, search the git history, run the test suite, linters,
type checkers, and builds, read logs, and make read-only fetches (for example
`gh issue view`, `gh pr view`, `git fetch`).

Do not: push; commit; open or comment on a pull request; run `gw pr open`;
comment on, assign, or transition the Linear ticket or the GitHub issue; post to
Slack or any other external service; or modify this project's source.

Report your findings here, in this session, as your reply — do not write them to
a file. Cover:

- what this ticket is really asking for
- what the relevant code does today, with `file:line` references
- the options you see, with the trade-offs of each
- your recommendation
- anything ambiguous or underspecified, flagged rather than guessed
{focus}
